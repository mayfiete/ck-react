import { PROCESSES } from '../shared/processes';
import * as ActionTypes from './ActionTypes';

export const Processes = (state = PROCESSES, action) => {
    switch (action.type) {
        default:
            return state;
    }
};