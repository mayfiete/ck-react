import { ORGANIZATIONS } from '../shared/organizations';

export const Organizations = (state = ORGANIZATIONS, action) => {
    switch (action.type) {
        default:
            return state;
    }
};